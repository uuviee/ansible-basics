---
  - name: Add lines
    hosts: localhost
    tasks: 
      - name: Adding lines
        lineinfile:
          path: /tmp/newfile.txt
          line: "{{ item }}"
        with_items:
        - 'This is line 1'
        - 'This is line 2'