---
  - name: Changing SELinux to disabled
    hosts: localhost
    tasks: 
      - name: Disable SELinux
        lineinfile:
          path: /etc/selinux/config
          regexp: '^SELINUX='
          line: SELINUX=disabled
      - name: reboot system
        command: reboot